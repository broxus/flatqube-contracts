pragma ever-solidity >= 0.62.0;

interface IOrderFactoryCallbacks {
    function onOrderRootDeployedCallback(
        address orderRoot,
        address token,
        address sendGasTo
    ) external;
}