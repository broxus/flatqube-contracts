pragma ever-solidity >= 0.62.0;

interface IOrderRootEvents {
    event CreateOrder(
        address order,
        address spentToken,
        uint128 spentAmount,
        address receiveToken,
        uint128 expectedAmount
    );
    event CreateOrderReject();
    event OrderRootCodeUpgraded(uint32 newVersion);
}