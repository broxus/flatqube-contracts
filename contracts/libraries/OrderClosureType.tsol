pragma ever-solidity ^0.62.0;

library OrderClosureType {
    uint8 constant FULL_EXCHANGE = 1;
    uint8 constant PART_EXCHANGE = 2;
    uint8 constant CHANGE        = 3;
    uint8 constant AMM           = 3;
    uint8 constant FULL_MATCHING = 4;
    uint8 constant PART_MATCHING = 5;
    uint8 constant CANCEL        = 6;
}
