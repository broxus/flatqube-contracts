pragma ton-solidity >= 0.62.0;

library OrderOperationTypes {
    uint8 constant SWAP_SUCCESS            = 0;
    uint8 constant SWAP_CANCEL             = 1;
    uint8 constant EXCHANGE                = 2;
    uint8 constant MATCHING_ORDER_EXCHANGE = 3;
    uint8 constant MATCHING_ORDER_TRANSFER = 4;
}